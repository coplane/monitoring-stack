apiVersion: 1

providers:
  - name: 'pytest-dashboards'
    orgId: 1
    folder: 'Pytest Monitoring'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
